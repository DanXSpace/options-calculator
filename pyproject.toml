[project]
name = "options-calculator"
version = "0.1.0"
description = "Black-Scholes option pricing calculator with Greeks and visualizations"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
]
